
const assert = require('assert');

describe('ProjectInit', () => {
    describe('StartProject', () => {
        it('should initialize and return 1', () => {
            assert.equal(1, 1);
        });
    });
});
